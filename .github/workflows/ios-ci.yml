# iOS CI temporarily disabled
# Requires macOS runner with Xcode 15+
# Enable when macOS runner is configured
name: iOS CI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    if: false
    steps:
      - run: echo "disabled"